<!doctype html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Hero Carousel Images • Kenari Tower Hotel</title>

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">

  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      min-height: 100vh;
      font-family: 'Inter', sans-serif;
      background:
        radial-gradient(1200px 600px at 10% 10%, rgba(99, 102, 241, .15), transparent 40%),
        radial-gradient(800px 500px at 90% 90%, rgba(16, 185, 129, .12), transparent 40%),
        linear-gradient(180deg, #0b1220, #020617);
      color: #e5e7eb;
      padding: 24px;
    }

    .shell {
      max-width: 1100px;
      margin: 0 auto;
    }

    /* ✅ SAME TOPBAR STYLE AS DASHBOARD */
    .topbar {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      padding: 16px 18px;
      border-radius: 20px;
      background: rgba(15, 23, 42, 0.70);
      backdrop-filter: blur(12px);
      border: 1px solid rgba(255, 255, 255, 0.08);
      box-shadow:
        0 18px 40px rgba(0, 0, 0, .35),
        inset 0 1px 0 rgba(255, 255, 255, .03);
    }

    .brand h1 {
      margin: 0;
      font-size: 16px;
      font-weight: 600;
      letter-spacing: .3px;
    }

    .brand p {
      margin: 4px 0 0;
      font-size: 12px;
      color: #9ca3af;
    }

    .userbox {
      display: flex;
      align-items: center;
      gap: 12px;
      flex-wrap: wrap;
      justify-content: flex-end;
    }

    .pill {
      padding: 8px 10px;
      border-radius: 999px;
      border: 1px solid rgba(255, 255, 255, .10);
      background: rgba(2, 6, 23, .55);
      font-size: 12px;
      color: #cbd5f5;
      white-space: nowrap;
    }

    .btnGhost {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 10px 12px;
      border-radius: 14px;
      background: rgba(255, 255, 255, .06);
      border: 1px solid rgba(255, 255, 255, .10);
      color: #e5e7eb;
      font-weight: 600;
      font-size: 12px;
      text-decoration: none;
      transition: all .2s ease;
      cursor: pointer;
    }

    .btnGhost:hover {
      transform: translateY(-1px);
    }

    .logoutBtn {
      border: none;
      cursor: pointer;
      padding: 10px 12px;
      border-radius: 14px;
      background: rgba(239, 68, 68, .10);
      border: 1px solid rgba(239, 68, 68, .25);
      color: #fecaca;
      font-weight: 600;
      font-size: 12px;
      transition: all .2s ease;
    }

    .logoutBtn:hover {
      transform: translateY(-1px);
      background: rgba(239, 68, 68, .14);
      border-color: rgba(239, 68, 68, .35);
    }

    .card {
      margin-top: 18px;
      background: rgba(15, 23, 42, 0.70);
      backdrop-filter: blur(12px);
      border: 1px solid rgba(255, 255, 255, 0.08);
      border-radius: 20px;
      padding: 18px;
      box-shadow:
        0 18px 40px rgba(0, 0, 0, .35),
        inset 0 1px 0 rgba(255, 255, 255, .03);
    }

    .heroRow {
      margin-top: 14px;
      padding: 16px;
      border-radius: 18px;
      background: rgba(2, 6, 23, .60);
      border: 1px solid rgba(255, 255, 255, .08);
    }

    .rowGrid {
      display: grid;
      grid-template-columns: 110px 1fr;
      gap: 14px;
      align-items: start;
    }

    .thumb {
      width: 110px;
      height: 78px;
      border-radius: 14px;
      overflow: hidden;
      border: 1px solid rgba(255, 255, 255, .12);
      background: rgba(255, 255, 255, .04);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 12px;
      color: #9ca3af;
    }

    .thumb img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    label {
      display: block;
      font-size: 12px;
      color: #cbd5f5;
      margin: 8px 0 4px;
    }

    input {
      width: 100%;
      padding: 10px 12px;
      border-radius: 12px;
      border: 1px solid rgba(255, 255, 255, .12);
      background: rgba(2, 6, 23, .60);
      color: #e5e7eb;
      font-size: 13px;
      outline: none;
    }

    .miniGrid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 12px;
    }

    .saveBar {
      margin-top: 16px;
      padding-top: 14px;
      border-top: 1px solid rgba(255, 255, 255, .08);
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 12px;
      flex-wrap: wrap;
    }

    .btnPrimary {
      padding: 12px 16px;
      border-radius: 14px;
      border: none;
      cursor: pointer;
      font-weight: 600;
      font-size: 12px;
      background: linear-gradient(135deg, #6366f1, #4f46e5);
      color: white;
      transition: all .2s ease;
    }

    .btnPrimary:hover {
      transform: translateY(-1px);
    }

    .statusOk {
      padding: 10px 12px;
      border-radius: 16px;
      font-size: 12px;
      background: rgba(16, 185, 129, .10);
      border: 1px solid rgba(16, 185, 129, .25);
      color: #bbf7d0;
    }

    .statusErr {
      padding: 10px 12px;
      border-radius: 16px;
      font-size: 12px;
      background: rgba(239, 68, 68, .12);
      border: 1px solid rgba(239, 68, 68, .30);
      color: #fecaca;
    }
  </style>
</head>

<body>
  <div class="shell">

    <div class="topbar">
      <div class="brand">
        <h1>Kenari Tower Hotel</h1>
        <p>Hero Carousel Images • CMS</p>
      </div>

      <div class="userbox">
        <div class="pill"><%= user?.email || "unknown" %></div>
        <div class="pill">Role: <%= user?.role || "-" %></div>

        <a class="btnGhost" href="/admin">← Dashboard</a>

        <form method="POST" action="/auth/logout" style="margin:0;">
          <button class="logoutBtn" type="submit">Logout</button>
        </form>
      </div>
    </div>

    <br>
    <div class="card">
      <div class="heroRow" style="margin-top:0;">
        <div style="display:flex;justify-content:space-between;align-items:center;gap:12px;flex-wrap:wrap;">
          <div>
            <div style="font-weight:600;">Add new hero slide</div>
            <div style="font-size:12px;color:#9ca3af;margin-top:4px;">Upload 1 image untuk menambah slide baru.</div>
          </div>
        </div>

        <form method="POST" action="/admin/hero-images/add" enctype="multipart/form-data" style="margin-top:12px;">
          <label>Upload Image</label>
          <input type="file" name="image" accept="image/*" required>

          <label>Alt Text (optional)</label>
          <input name="alt_text" placeholder="e.g. Hotel Lobby">

          <div style="margin-top:12px;display:flex;justify-content:flex-end;">
            <button class="btnPrimary" type="submit">+ Add Slide</button>
          </div>
        </form>
      </div>
      
      <br>
      <hr>
      <br>

      <form method="POST" action="/admin/hero-images" enctype="multipart/form-data">
        <div class="card">
          <div style="font-weight:600;">Update hero slide</div>
          <div style="font-size:12px;color:#9ca3af;margin-top:4px;">Silahkan edit gambar, order dan status slide.</div>
          <% (images || []).forEach((img, i) => { %>
          <div class="heroRow">
            <input type="hidden" name="images[<%=i%>][id]" value="<%= img.id %>">

            <div class="rowGrid">
              <div class="thumb">
                <% if (img.image_path) { %>
                <img src="<%= img.image_path %>" alt="preview"
                  onerror="this.remove(); this.parentElement.textContent='No Image';" />
                <% } else { %>
                No Image
                <% } %>
              </div>

              <div>
                <label>Upload Image (Slide <%= i + 1 %>)</label>
                <input type="file" name="image_<%= img.id %>" accept="image/*">

                <label>Alt Text</label>
                <input name="images[<%=i%>][alt_text]" value="<%= img.alt_text || '' %>">

                <div class="miniGrid">
                  <div>
                    <label>Sort Order</label>
                    <input name="images[<%=i%>][sort_order]" value="<%= img.sort_order %>">
                  </div>
                  <div>
                    <label>Active (1 / 0)</label>
                    <input name="images[<%=i%>][is_active]" value="<%= img.is_active %>">
                  </div>
                </div>

                <button
                  class="logoutBtn"
                  type="submit"
                  formaction="/admin/hero-images/<%= img.id %>/delete"
                  formmethod="POST"
                  onclick="return confirm('Hapus slide ini?')"
                  style="margin-top:12px; float:right; background:rgba(239,68,68,.10); border:1px solid rgba(239,68,68,.25); color:#fecaca;"
                >
                  Delete Slide
                </button>
                <div style="clear:both;"></div>

              </div>

            </div>
          </div>
          <% }) %>

          <div class="saveBar">
            <span style="font-size:12px;color:#9ca3af;">Upload hanya mengubah slide yang dipilih</span>

            <div style="display:flex; align-items:center; gap:10px;">
              <% if (saved) { %><div class="statusOk">✅ Saved successfully.</div><% } %>
              <% if (error) { %><div class="statusErr">❌ <%= error %></div><% } %>
              <button class="btnPrimary" type="submit">Save Hero Images</button>
            </div>
          </div>

        </div>
      </form>

    </div>
</body>

</html>